"use strict";

function soma(v1, v2){ // v1 e v2 são os parâmetros
    let r = v1 + v2;
    return r;
    // função com retorno
}

function lerNome(){
    let nome = prompt("Digite um nome");
    console.log(`Nome: ${nome}`);
}